# NagatoPix - Changelog

## 1.2.0

**从"单一账号的浏览下载器"到"多账号一体化 Pixiv 客户端"。**

本次版本引入多账号体系、主题系统、作品查看模态框，并将配置与会话数据从单一文件升级为结构化存储。

---

### Added

#### 多账号体系

- **账户标签页**：侧边栏新增入口，显示当前账号的头像、名称、作品/关注/收藏数
- **`accounts.json`**：独立存储所有关联账号的 RefreshToken 与缓存资料
  - `refresh_token`：每个账号的凭据
  - `profile`：缓存的用户资料
  - `following`：缓存的关注列表
  - `bookmarks`：缓存的收藏列表
  - `current_index`：当前激活的账号
- **添加账号**：账户页右下角 `+` 按钮，粘贴新 RefreshToken 后自动验证并切换
- **账号列表**：显示所有关联账号，当前账号高亮标记
- **账号切换**：一键切换激活账号，后端自动重新登录
- **移除账号**：需二次确认，移除后自动切到剩余账号
- **自动迁移**：首次启动时从 `config.toml` 的 `refresh_token` 迁移至 `accounts.json`
- **关注列表加载**：点击"关注"数字展开关注用户列表（头像、UID、用户名、账户名）
- **收藏列表加载**：点击"收藏"数字展开 Pixiv 公开收藏（表格形式，标题可点击打开模态框）

#### 作品查看模态框

- **点击列表标题**：打开模态框，不再跳转 Pixiv
- **左栏**：上方 large 尺寸图像（`600x1200`），下方分页导航（多页作品）
- **右栏**：作品详情 + 用户信息
  - 标题、徽章（NEW / AI / R-15 / R-18 / R-18G）
  - 浏览量、收藏数、页数、发布日期
  - 可点击标签（触发标签搜索）
  - 用户卡片（头像 + 用户名 + 账户名）
- **顶部切换**：基于列表序的"上一作品 / 下一作品"
- **键盘快捷键**：`←` / `→` 切换作品，`Shift + ←/→` 切换分页，`Esc` 关闭
- **点击外部关闭**

#### 标签增强

- **表格中标签可点击**：点击标签直接跳转搜索页，自动填充并执行完全匹配搜索
- **模态框内标签可点击**：同上
- **标签名提取**：自动剔除译名部分（`name(translated)` → `name`）
- **萌娘百科搜索**：搜索标签行新增"搜索百科"按钮，新标签页打开 `zh.moegirl.org.cn` 精确搜索

#### 模态框动作

- **Pixiv 书签**：模态框底部按钮，一键添加/移除 Pixiv 公开书签
- **下载**：模态框底部按钮，直接加入下载队列（复用列表元数据）

#### 主题系统

- **暗色 / 亮色主题切换**：侧边栏底部按钮
- **CSS 变量驱动**：所有颜色集中定义，切换即时生效
- **持久化**：主题选择保存到 `config.toml` 的 `theme` 字段
- **同步到后端**：切换后通过 WebSocket 通知后端保存

#### 用户背景图

- **查看用户时自动显示背景**：使用该用户的 `background_image_url` 作为页面背景
- **仅在用户详情页生效**：切换到其他标签页时背景隐藏
- **卡片半透明**：有背景图时，页面上的卡片（panel、用户卡片、下载面板）自动切换为 70% 不透明 + 背景模糊

#### 会话持久化

- **`session.json`**：合并原有的 `queue.json` 与 UI 状态
  - `queue`：未处理任务 + 失败任务
  - `ui_state`：当前标签页、主题、各列表的 items
- **刷新页面恢复**：重新加载后自动还原之前的搜索结果、排行榜、关注新作、用户详情
- **`queue.json` 自动迁移**：首次启动时若存在旧文件，自动合并到 `session.json` 后删除

#### SVG 图标系统

- **`webui/ui_icons/`**：独立的 SVG 图标目录
- **侧边栏图标**：下载、搜索、加号、账户、用户详情、设置、排行榜、推荐、关注新作
- **动作图标**：`download.svg`、`bookmark_pixiv.svg`、`search_ext.svg`、`expand.svg`
- **折叠图标**：`expand.svg` + CSS 旋转（收起时 -90°）
- **主题适配**：通过 CSS `filter` 控制颜色，自动适配暗色/亮色主题

#### 复选框多选

- **表格每行前置复选框**：`unselected.svg` / `selected.svg`
- **点击复选框切换选中**：不再点整行选中
- **Shift 范围选择**：点击第一个复选框后，Shift + 点击另一个，中间所有行批量选中/取消
- **悬浮球联动**：选中数量实时显示
- **列表重渲染保留选中**：基于 PID 匹配

#### 元数据复用

- **队列项携带 metadata**：从列表加入下载队列时，自动附带 `_slim_illust`（精简的 illust 对象）
- **下载时跳过 API 调用**：`DownloadWorker._process_illust` 检测到缓存元数据时直接复用
- **API 调用量降低**：批量下载场景下减少 30%~50% 的 `illust_detail` 请求
- **`_slim_illust`**：仅保留下载与查看所需字段（id、title、user、caption、tags、meta_pages 等），传输体积降低 80%+

#### 排行榜新作标识

- **对比今日与昨日排行榜**：固定 480 条，自动检测新出现的作品
- **NEW 徽章**：新增作品显示绿色徽章，与其他徽章同排
- **昨日数据不可用时**：不显示任何 NEW 徽章（避免误标）

---

### Changed

#### 用户详情页

- **半透明卡片**：有背景图时自动调整
- **关注按钮**：直接关注 / 取消关注，状态实时同步
- **标签云折叠**：只保留 chip 筛选，移除筛选输入框

#### 账户页

- **重写为账户中心**：头像 + 统计 + 关注列表 + 收藏列表
- **统计数字可点击**：点击后展开对应列表

#### 表格结构

- **复选框列前置**：作为第一列
- **事件委托**：所有交互通过 `data-action` + 捕获阶段监听，避免内联 `onclick` 的转义问题
- **行点击不再切换选中**：只有复选框触发选中

#### 模态框

- **尺寸自适应**：`min-height: 0` 保证 flex 子项正确收缩，图像不截断
- **单页作品**：`original` 自动转为 `master1200.jpg`（原图可能是 PNG，但 master1200 固定为 JPG）
- **加载失败回退**：master1200 失败时自动回退到原图
- **多页作品**：优先使用 `meta_pages.image_urls.large`

#### 图标系统

- **Emoji 替换为 SVG**：侧边栏、动作按钮、折叠图标全部迁移

#### 主题变量

- **CSS 变量集中管理**：`:root[data-theme="dark"]` 与 `:root[data-theme="light"]` 定义两套颜色
- **切换无闪烁**：所有颜色通过变量引用

---

### Removed

- **用户浏览与显示设置**：Pixiv API 不支持修改用户资料，移除相关 UI 与后端逻辑
- **单账号模式**：完全迁移到多账号架构
- **`queue.json`**：合并到 `session.json`
- **旧版 `profile_settings` 相关字段**：性别、地区、生日、主页、Twitter
- **Emoji 图标**：全部替换为 SVG

---

### Fixed

- **表头滚动错位**：回退到基础 `<table>` 结构，放弃 `position: sticky` 尝试
- **收藏数筛选**：从前端参数传入改为客户端实时过滤（API 参数需要 Pixiv Premium）
- **关注新作卡在 270 条**：`next_url` 解析真实 offset，加去重检测与迭代上限
- **`follow_result` 命名冲突**：关注用户与关注新作改用不同的消息类型（`follow_user_result` / `follow_new_result`）
- **关注新作不显示**：`_slim_illust` 精简字段，避免 WebSocket 消息过大导致渲染失败
- **模态框图像截断**：flex 子项加 `min-height: 0`
- **用户头像不显示**：`format_item` 补充 `author_avatar` 字段，回退链到 `_slim_illust.user`
- **头像 onerror 泄漏**：移除内联 SVG data URI，改为最简 `removeAttribute('src')`
- **`original` 拼接 404**：单页作品统一转为 `_master1200.jpg`，不再沿用原扩展名
- **`resetUserDetailView` 空指针**：清理对已删除 DOM 元素的引用
- **`doUserSearch` 空指针**：合并到 search 标签页后更新容器 id
- **排行榜昨日数据为空**：改用 today 请求返回的实际 `date` 减一天作为 yesterday 日期，并按 PID 去重统计

---

### Performance

标签页内存占用测试结果（会话全内容 + 背景图 + 作品模态框）：

| 场景 | 内存 |
| --- | --- |
| 空闲（无背景图） | ~120 MB |
| 加载 1500 条搜索结果 + 打开模态框 | < 200 MB |
| 对比：Pixiv 官方 WebAPP（20 作品） | ~3.6 GB |

---

## 1.1.0

### Added

- **侧边栏导航**：功能标签页移至左侧，纵向布局，一目了然
- **动态悬浮球**：仅在选中作品时出现，实时显示选中数量，一键加入下载队列
- **下载标签页**：
  - 实时进度条（已处理 / 总数）
  - 详细统计面板（待处理、已处理、失败、工作线程、运行状态）
  - 预设模式切换：「普通」（单线程）与「高速」（多线程并行）
  - 线程数实时调节（1–8）
- **搜索设置折叠菜单**：
  - 检索范围：标签（部分一致 / 完全一致）/ 标题与说明
  - 投稿时间：不限 / 最近一天 / 最近一周 / 最近一月 / **自定义日期范围**
  - 作品类型：插画 / 漫画（动图已排除）
  - 收藏数范围筛选（客户端过滤，实时生效）
- **合并搜索**：作品搜索与用户搜索合并至单一标签页，通过下拉选单切换
- **标签云 / 作者云筛选**：可折叠展开，从已获取作品自动生成高频标签与作者
- **排行榜新作标识**：对比今日与昨日排行榜，新增作品显示 `NEW` 徽章
- **队列自动启动**：启动程序时，如队列中存在已保存的任务，立即开始处理
- **队列实时保存**：每个任务完成后立即写入 `queue.json`，意外退出不丢失进度
- **下载标签页加号按钮**：手动下载与收藏夹导入合并为一个模态框入口

### Changed

- **项目更名**：NagatoDownloader → NagatoPix
- **架构升级**：从"下载工具"重新定位为"Pixiv 客户端"
- **搜索上限提升**：单次搜索最大 6000 条（200 页 × 30 条）
- **新作单次请求**：从 30 条提升至 300 条（10 页批量获取）
- **排行榜固定返回**：480 条（今日榜 + 昨日榜对比）
- **作品标识位置**：从独立列移至标题后，作为内联徽章显示
- **下载标签页位置**：移至「设置」之前，紧随其他功能标签页
- **元数据换行**：`<br/>` 正确转换为 `\n` 并保留至元数据（通过 ExifTool CStr 模式传递）
- **日志简化**：移除启动 banner 分隔线
- **表头行为**：回归基础表格，表头随内容滚动（放弃 sticky 尝试）

### Removed

- **小说获取与下载**：专注插画与漫画
- **"最热"排序**：该排序依赖 Pixiv Premium 会员，移除以避免误导
- **API 语言设置**：经测试该参数无效，移除相关配置
- **手动下载 / 收藏夹导入独立标签页**：合并至下载标签页的模态框
- **用户搜索独立标签页**：合并至搜索标签页
- **标签筛选输入框**：仅保留标签云 / 作者云
- **底部菜单**：功能入口全部迁移至侧边栏
- **上一页 / 下一页按钮**：搜索改为从起始页自动向后步进

## 1.0.1-snapshot1

- Tweak some Filenames and translations
- 调整了一些文件名和翻译

## 1.0.0

- Inital Release
- 最初发行
